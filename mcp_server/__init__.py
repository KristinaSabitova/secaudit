"""secaudit as an MCP tool: a thin wrapper over the deployed REST API.

Deliberately not named `mcp/` — a directory by that name in the repository root
would shadow the official `mcp` SDK package on sys.path.
"""
